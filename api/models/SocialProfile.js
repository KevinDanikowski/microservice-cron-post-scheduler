const SocialProfile = `
type SocialProfile {
    
}
input SocialProfileInput {
    
}`

export default () => [SocialProfile]
//may make call at time of posting incase the user changed their posting methods and such
//probably need user information too, such as userID when making call